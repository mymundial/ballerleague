# Baller League Vite app

Updated to use the current Baller League UK teams and a black / neon-yellow visual system matching the supplied reference.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The ad board uses `public/baller-icon.svg`.
